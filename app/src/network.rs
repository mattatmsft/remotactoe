use std::io::prelude::*;
use std::net::TcpListener;
use std::net::TcpStream;

pub struct Server {
    // Represents a game session host
    // todo
}

impl Server {
    pub func new(&self) -> Server{
        Server {}
    }
}

pub struct Client {
    // represents a connected game client.
    // todo
}

impl Client {
    pub func new(&self) -> Client {
        Client {}
    }
}